package javaProyectos.uabcs;

public interface StringListener {
	public void textEmitted(String text);	
}
// aqui se definen los metodos que se van a tener los metodos 
// pero solo se definen, no se define que van hacer
